# Serial Flash

这是 `Memory > Serial Flash` 的目录骨架。

后续计划放入：

- `serial-flash` 模板草案
- Datasheet 常见参数字段词表
- DigiKey / Mouser / 原厂字段别名对齐
- 典型样本映射，如 `GD25Q128E`、`W25Q128FV`

这里默认面向 `Serial NOR Flash`，后续如果需要再细分 `SPI NOR`、`QSPI NOR`、`Octal NOR`。
